import React from 'react';
import { createRoot } from 'react-dom/client';
import App from './App'; // Replace 'App' with your root component name

createRoot(document.getElementById('root')).render(<App />);
